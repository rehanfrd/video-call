name: zing_call
description: ZegoCloud Audio/Video Calling App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Yahan se humne '^' hata diya hai, taaki ye strictly stable version use kare
  zego_uikit_prebuilt_call: 4.1.9

flutter:
  uses-material-design: true
