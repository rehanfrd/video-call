name: zing_call
description: ZegoCloud Audio/Video Calling App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 🔥 Latest version updated here to fix Dart compile errors
  zego_uikit_prebuilt_call: ^4.24.4

# 🔥 YEH OVERRIDE KOTLIN ERROR KO ROKEGA 🔥
dependency_overrides:
  is_lock_screen2: 2.0.2

flutter:
  uses-material-design: true
